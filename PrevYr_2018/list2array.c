#include"node.h"
#include<stdio.h>
#include<stdlib.h>
extern int *Arr[N];
extern int Num_Elements[N];
void ConvertListstoArray(Locality *start)
{
    // int index = 0;
    for (int i = 0 ; i < N ; i++)
    {
        Num_Elements[i] = start->count;
        

    }


}
